package Cars;

public class Kia extends Car {
    public Kia(String model, int year, String color, boolean isAutomatic, double engineVolume) {
        super("Kia", model, year, color, isAutomatic, engineVolume);
    }
}