package Cars;

public class Lada extends Car {
    public Lada(String model, int year, String color, boolean isAutomatic, double engineVolume) {
        super("Lada", model, year, color, isAutomatic, engineVolume);
    }
}
